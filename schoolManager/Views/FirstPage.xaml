<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        xmlns:d="http://xamarin.com/schemas/2014/forms/design"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Class="schoolManager.Views.FirstPage"
        Title="FirstPage">
    <ContentPage.BindingContext>
        <viewModels:FirstViewModel />
    </ContentPage.BindingContext>
    <VerticalStackLayout Spacing="10" Margin="10">
        <Label Text="{Binding Baptiste.Nom}" />
        <Label Text="{Binding Baptiste.Prenom}" />
        <ListView ItemsSource="{Binding Baptiste.Evaluations}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label Text="{Binding Appreciation}" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Label Text="{Binding Message}" />
    </VerticalStackLayout>

</ContentPage>